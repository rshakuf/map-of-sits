<Window x:Class="map_os_sits.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:map_os_sits"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,10">
            <Button Content="בצע" Click="ConfirmSelection_Click" Width="80" Margin="0,0,10,0"/>
            <TextBlock x:Name="AvailableCountText" VerticalAlignment="Center" FontWeight="Bold"/>
        </StackPanel>

        <ScrollViewer Grid.Row="1">
            <WrapPanel x:Name="SeatsPanel" />
        </ScrollViewer>

    </Grid>
</Window>
